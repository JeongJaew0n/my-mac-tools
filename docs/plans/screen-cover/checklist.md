# checklist — screen-cover

## 0. 검토
- [x] 모든 디스플레이 열거·배율 확인 (3대, 배율 상이)
- [x] 창 레벨로 메뉴 막대·Dock 을 덮을 수 있는지 확인 (`CGShieldingWindowLevel()`)
- [x] 인프로세스 디스플레이 슬립 차단 확인 (`IOPMAssertionCreateWithName`)
- [x] 인증 수단 조사 후 **넣지 않기로** 결정
- [x] spec · context 작성

## 1. 커버
- [x] `ScreenCoverManager` — 화면별 창 생성·해제
- [x] `CGShieldingWindowLevel()`, `canJoinAllSpaces`, `stationary`, `fullScreenAuxiliary`
- [x] borderless 창이 key 가 되도록 `canBecomeKey` 열기
- [x] 이미지 로드 실패 시 커버를 띄우지 않고 사유를 표시 (하네스로 확인)

## 2. 이미지
- [x] 파일 선택(`NSOpenPanel`) · 경로를 `UserDefaults` 에 저장
- [x] 꽉 채우기 / 맞추기 선택, 기본값 꽉 채우기 (기본값 하네스로 확인)
- [ ] 배율이 다른 화면에서 각각 의도대로 보이는지 확인 — **눈으로 확인 필요**

## 3. 해제
- [x] 해제 버튼 — **모든 화면에** 표시 (코드상. 실제 확인 필요)
- [x] `Enter` 2번 — 상한 1.5초
- [x] `Esc` 2번 — `Enter` 와 같은 상한 1.5초 (2026-09-16 변경)
- [x] 셋 다 `stop()` 한곳으로 모음

## 4. 슬립 차단
- [x] 커버 시작 시 assertion 획득, 해제 시 반납
- [ ] `pmset -g assertions` 로 획득·반납 확인
- [ ] 앱 강제 종료 후 잔존 0건 확인

## 5. 모니터 착탈
- [ ] `didChangeScreenParametersNotification` 구독
- [ ] 커버 중 모니터를 뽑으면 창이 사라지는지
- [ ] 커버 중 모니터를 꽂으면 창이 붙는지

## 6. 마무리
- [x] 세 번째 탭으로 편입, 탭 라벨 상태 점
- [x] 문자열 3언어(ko/en/ja), 키 집합 대조 (56개 일치)
- [x] README — **"잠금이 아니다"** 명시
- [ ] `Cmd-Tab` · 강제 종료 창이 커버 위로 오는지 확인 후 기록

## 남은 확인 — 실제로 덮어봐야 하는 것

창을 띄우지 않고 확인할 수 있는 것만 지금까지 확인했다. 아래는 3개 화면을 실제로 덮어야
알 수 있어 미확인 상태다.

- [ ] 화면 3개가 모두 덮이는지
- [ ] 메뉴 막대·Dock 이 가려지는지
- [ ] 꽉 채우기 / 맞추기가 배율 다른 화면에서 의도대로 보이는지
- [ ] 해제 3종이 실제로 동작하는지 (`Esc` 는 2번 누르기로 변경됨)
- [ ] 커버 중 `pmset -g assertions` 에 `MyMacTools screen cover` 가 보이는지
- [ ] 커버 중 모니터 착탈에 창이 따라오는지
- [ ] `Cmd-Tab` · 강제 종료 창이 커버 위로 오는지
- [ ] `Cmd-Q` 로 빠져나올 수 있는지 (탈출구가 살아 있는지)

## 7. 전역 단축키 (2026-09-16 추가 요청)

- [x] Carbon `RegisterEventHotKey` 채택 — 손쉬운 사용 권한 없이 `noErr` 로 등록되는 것 확인
- [x] `Shortcut` 모델 — 표시 문자열·Carbon 수정자 변환·저장
- [x] 수정자(`⌘`/`⌥`/`⌃`) 없는 조합 거절
- [x] 기록 UI — `Delete` 로 지움, `Esc` 로 취소
- [x] **덮기만** 한다 (해제는 화면의 세 가지)
- [x] 덮지 못하면 창을 앞으로 내보내 이유를 보여줌
- [ ] 실제로 키를 눌러 발동하는지 — **직접 확인 필요**
- [ ] 다른 앱이 떠 있을 때도 발동하는지 — **직접 확인 필요**

시스템이 이미 쓰는 조합(`⌘Space` 등)은 등록이 `noErr` 로 성공해도 시스템이 먼저 가져간다.
앱에서 감지할 방법이 없어 README 에 안내만 남겼다.
