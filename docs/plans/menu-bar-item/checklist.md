# checklist — menu-bar-item

`(확인)` 은 사용자가 직접 써보고 확인해준 것, `(실측)` 은 명령·캡처로 확인한 것이다.

## 0. 검토
- [x] `MenuBarExtra` 를 이 타깃(macOS 14)에서 쓸 수 있는지 (실측 — 컴파일 통과)
- [x] `NSStatusItem` 에 권한이 필요 없는지 (실측 — 생성 성공)
- [x] 아이콘 후보 SF Symbol 존재 확인 (실측)
- [x] Dock 유지 / 드롭다운 / 상태 반영 — 사용자에게 물어 확정
- [x] spec · context 작성

## 1. 메뉴바
- [x] `NSStatusItem` 직접 (`MenuBarExtra` 는 버튼을 못 내줘서 기각)
- [x] 글리프 높이를 16pt 로 못 박음 — `pointSize: 16` 이 22pt 를 내놔 잘렸다 (실측)
- [x] 상태 막대에 아이콘이 뜬다 (실측 — 메뉴바 캡처로 확인)
- [x] 돌고 있으면 글리프 우측 하단에 초록 점, 아니면 아무것도 없음 (실측 — 켜고/끄고 캡처 비교)
- [x] Dock 아이콘 유지 (`LSUIElement` 없음 확인)

## 2. 메뉴 내용
- [x] 기능 3개를 체크마크(`NSMenuItem.state`)로 — 색 점은 메뉴 template 렌더링에 죽으므로 쓰지 않음
- [x] 사진이 없으면 화면 가리기 항목 비활성
- [x] `창 열기` — `WindowGroup` 에 id 부여 후 `openWindow`
- [x] `종료` — `NSApp.terminate` 로 보내 덮개 경고를 거치게
- [ ] 메뉴를 실제로 열어 항목이 의도대로 보이는지 — **직접 확인 필요**
- [ ] 각 토글이 실제로 켜고 끄는지 — **직접 확인 필요**
- [ ] 닫았던 창이 `창 열기` 로 다시 열리는지 — **직접 확인 필요**

## 3. 규칙 정리
- [x] 덮개 켤 때 "끝나면 Mac잠자기 모드" 내리는 규칙을 `Actions` 한곳으로
      (창과 메뉴 두 곳에서 켤 수 있게 되므로 갈라지기 전에)

## 4. 바뀐 기존 동작
- [x] 창을 닫아도 잠자기 방지 세션이 유지된다 (`onDisappear` 의 `stop()` 제거)
- [ ] 실제로 창을 닫고 유지되는지 — **직접 확인 필요**

## 5. 마무리
- [x] 문자열 3언어, 키 집합 대조 (62개 일치)
- [x] 빌드·서명·설치
- [ ] README
